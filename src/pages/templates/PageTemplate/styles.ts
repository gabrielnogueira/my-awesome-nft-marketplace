import styled from "styled-components";

export const PageTemplate = styled.div.attrs({
  className: `flex flex-col w-full `,
})``;

export const Content = styled.div.attrs({
  className: `flex flex-col w-full  p-4 px-14`,
})``;
